using UnityEngine;
using System.Collections;

/*
public class Rigidtester : MonoBehaviour
{
	public GameObject imitates;

	void Update()
	{
		rigidbody.MovePosition(imitates.transform.position);
		//rigidbody.MoveRotation(imitates.transform.rotation);

		Quaternion rotation = imitates.transform.rotation;
		rotation = rotation * Quaternion.Euler(-90, 0, 0);
		rigidbody.MoveRotation(rotation);
	}
}
*/